# TheBibleMark
This is just a c# implementation of BibleMark made in ProtoFlux inside of Resonite. Just a fun experiment to compare performance of Protoflux to c# (The language ProtoFlux is written in)
build with `dotnet build -c Release`
